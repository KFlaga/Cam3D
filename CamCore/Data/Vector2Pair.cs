namespace CamCore
{
    public struct Vector2Pair
    {
        public Vector2 V1;
        public Vector2 V2;

        public override string ToString()
        {
            return "V1: [" + V1.ToString() + "], V2: [" + V2.ToString() + "]";
        }
    }
}
